#include <bits/stdc++.h>
using namespace std;
#define ll long long

void solve () {
    string s, t;
    cin >> s >> t;

    cout << s.size() << " " << t.size() << "\n";
    cout << s << " " << t << "\n";  
}

int main() {
    ios::sync_with_stdio(false);cin.tie(nullptr);
    solve ();
    return 0;
}
